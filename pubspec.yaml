name: mlkit_fluter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_face_detection: ^0.9.0
  camera: ^0.10.5+5
  permission_handler: ^11.1.0
  flutter_hooks: ^0.18.0
  image: ^4.0.19


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true


  assets:
    - assets/spiderman.png
    - assets/devil.png  # L'indentation doit être exactement comme ça avec 2 espaces
    
